/* TELA-NAO-IMPLEMENTADA

   Esboço. A tela `configuracao` é implementada numa tarefa própria, que
   substitui este arquivo inteiro. Ele existe para que a página não
   referencie um 404. */
Api.aoCarregar(async () => {
  Api.vazio("conteudo", "Tela em construção.");
});
