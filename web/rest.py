"""DRF viewsets — the CRUD API mounted at ``/api/v1/``.

The ``AlertViewSet`` now lives in ``alerts/rest.py`` and ``MeView`` in
``accounts/rest.py``. Catalogue (games/genres) and library viewsets live in
``catalog/rest.py``; forum (topics/replies/comments) viewsets live in
``community/rest.py``. Nothing remains here for now.
"""
