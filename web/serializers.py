"""DRF serializers for the CRUD API (``/api/v1/``).

Alert and UserProfile serializers now live in ``alerts/serializers.py`` and
``accounts/serializers.py``. Catalogue (games/genres/library) serializers
live in ``catalog/serializers.py``; forum serializers live in
``community/serializers.py``. Nothing remains here for now.
"""
